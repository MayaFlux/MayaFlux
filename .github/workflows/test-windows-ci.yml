name: "Test Windows CI (manual)"
on:
  workflow_dispatch:
jobs:
  test:
    uses: ./.github/workflows/build-windows.yml
    with:
      version: "0.4.0-ci-test"
      clean_version: "0.4.0"
      is_dev: "1"
      run_tests: false
      create_distribution: true
      artifact_name: "MayaFlux-ci-test-windows-x64"
      distribution_name: "windows-x64"
      os_display_name: "Windows"
